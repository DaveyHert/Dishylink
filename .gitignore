node_modules
dist
dist-electron
release
.output
.wxt
.DS_Store

collector/data

# Vitest browser mode writes failure screenshots next to the test file, and
# attachments (diffs, traces) under .vitest-attachments. Diagnostic, not source.
**/__screenshots__/
.vitest-attachments/

# Local Starlink account session for the dev cloud proxy (never commit).
.starlink-cookie

# Rasterized from build/icon.svg by scripts/make-icon.mjs. The SVG is the only
# committed form of the mark.
build/icon.icns
build/icon.png

# Rendered from the tones in src/lib/alertSound.ts by scripts/make-alert-sounds.mjs,
# so the chime the tray plays cannot drift from the one the dashboard synthesises.
build/sounds/
